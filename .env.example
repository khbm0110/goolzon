# Filled in during the FINAL phase, when we connect Appwrite.
# Never commit the real .env.local file, and never paste real keys into chat.

NEXT_PUBLIC_APPWRITE_ENDPOINT=
NEXT_PUBLIC_APPWRITE_PROJECT_ID=
# Server-only secret — used only in API routes / server actions, never sent to the browser.
APPWRITE_API_KEY=
